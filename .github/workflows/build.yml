name: Build iStoreOS for JDCloud AX1800 Pro

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Maximize space
      uses: easimon/maximize-build-space@master
      with:
        swap-size-mb: 4096
        remove-dotnet: "true"
        remove-android: "true"
        remove-haskell: "true"

    - name: Install Dependencies
      run: |
        sudo apt update
        sudo apt install -y \
        build-essential ccache ecj fastjar file g++ gawk gcc-multilib \
        genisoimage gettext git java-propose-classpath libelf-dev \
        libncurses5-dev libncursesw5-dev libssl-dev python3 \
        python3-pyelftools qemu-utils subversion swig time xsltproc \
        zlib1g-dev curl wget

    - name: Clone iStoreOS source
      run: |
        git clone --depth 1 https://github.com/iStoreOS/istoreos.git openwrt
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Apply config
      run: |
        cp .config openwrt/.config
        cd openwrt
        make defconfig

    - name: Build firmware
      run: |
        cd openwrt
        make -j$(nproc) || make -j1 V=s

    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: JDCloud-AX1800Pro-Firmware
        path: openwrt/bin/
